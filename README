PythonFileSorter
====
Description:
sorts files in it's own extension directory.
windows only (might work on linux dunno.)
====
Usage:
open up a windows command prompt
type python pfs-1.0.py
voila :0 sorted files in its own directory .py in folder .py, and so on.

