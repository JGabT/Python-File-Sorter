PythonFileSorter
====
description:
scans directories for new files and sorts it to it's own extension folder
===
example:
user downloads example.exe then chrome auto saves it to Downloads folder (i hate this)
PythonFileSorter creates an .exe folder for example.exe and moves it.

